<div class="container">
	<h1>Books Index</h1>
	<%= link_to 'Add Book', new_book_path %>
	<ul>
		<% @books.each do |book| %>
		<li><%= link_to book.title, book_path(book) %></li>
		<% end %>
	</ul>
</div>
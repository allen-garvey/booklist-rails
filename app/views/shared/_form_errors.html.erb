<% if item and item.errors.any? %>
	<div class="alert alert-danger">
		<h2>
			<%= pluralize(item.errors.count, "error") %> prevented this <%= name %> from being saved:
		</h2>
		<ul>
			<% item.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>